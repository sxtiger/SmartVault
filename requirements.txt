# ============================================================
# SmartVault / 智能仓库 依赖清单
# 建议：macOS + Apple Silicon，使用 Python 3.12 虚拟环境安装：
#   brew install python@3.12 ffmpeg
#   /opt/homebrew/bin/python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# ============================================================

# ---- Web 服务（模块 B：FastAPI 本地 RAG）----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# ---- 守护进程（模块 A）----
watchdog>=4.0.0
requests>=2.32.0

# ---- 多模态解析 ----
ocrmac>=0.5.0              # macOS Vision 框架 OCR（Neural Engine 加速）
PyMuPDF>=1.24.0            # PDF 文本提取（含 iWork Preview.pdf 与扫描页渲染）
rapidocr>=3.0.0            # 手写中文 OCR（PP-OCRv6 模型内置 wheel，离线可用；PDF 扫描页）
python-docx>=1.1.0         # .docx
openpyxl>=3.1.0            # .xlsx
python-pptx>=0.6.23        # .pptx

# ---- 语音识别（Metal 加速优先，二选一或都装）----
mlx-whisper>=0.4.0         # Apple MLX 后端（Metal GPU，M4 Pro 推荐）
# openai-whisper>=20240930 # 备选 CPU 后端（需要 ffmpeg）

# ---- RAG 检索链路 ----
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.4
chromadb>=0.5.0
sentence-transformers>=3.0.0
torch>=2.4.0

# ---- 菜单栏控制台（menu_bar_app.py）----
rumps>=0.4.0               # macOS 状态栏 UI（PyObjC 封装）

